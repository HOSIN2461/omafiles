# nautilus-dropbox's "View on Dropbox.com" — the share link, opened in the
# browser instead of copied.
name = "View on Dropbox.com"
command = "omafiles-dropbox-link open %f"
each = true
requires = ["omafiles-dropbox-link", "socat"]
under = ["~/Dropbox"]
